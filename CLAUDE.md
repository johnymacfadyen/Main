# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current state

This repository is currently **empty** — it has no source code, build configuration,
or commit history yet (this file is the starting point). The sections below are
placeholders to fill in as the codebase takes shape. Replace each one with concrete,
verified details — do not leave guesses in place.

When real code is added, regenerate this file from the actual project so future Claude
Code instances get accurate guidance. A good time to do that is right after the initial
project scaffold lands (package manifest, build config, first source files).

## Build, test, and run commands

_To be filled in once the project has a build system._ Capture here:

- How to install dependencies
- How to build the project
- How to run the linter / formatter
- How to run the full test suite, and how to run a single test

## Architecture and structure

_To be filled in once there is code to describe._ Focus on the "big picture" that
requires reading multiple files to understand — module boundaries, data flow, and key
abstractions — rather than restating the directory tree.

## Conventions

_To be filled in._ Record project-specific conventions (naming, patterns, required
checks, commit/PR expectations) once they are established. Omit generic best practices.
